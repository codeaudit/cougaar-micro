The ImagePlugIn requires a data directory for temporary storage of pictures.
The data directory must be a subdirectory of the directory which is used to execute the node that ImagePlugIn is in. 
The ImagePlugIn has dependencies on the gphoto open source software package which runs on Linux.